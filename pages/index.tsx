import React from 'react';
import Layout from '../components/layout';

export const Index = (): JSX.Element => {
  return (
    <Layout>
      [WIP: personal wordpress site thdk.be is being converted to a git powered
      nextjs site]
    </Layout>
  );
};

export default Index;
